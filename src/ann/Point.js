/**
 * Two dimensional point with an associated value
 *
 * @export
 * @class Point
 */
export default class Point {
  constructor(x, y, value) {
    this.x = x
    this.y = y
    this.value = value
  }
}
